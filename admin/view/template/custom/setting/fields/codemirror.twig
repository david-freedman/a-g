<div {{ popover }}>
    {% if error %}
        <div class="text-danger">{{ error }}</div>
    {% endif %}
    <textarea data-code="{{ values_options.code }}" name="custom_setting{{ name }}" id="input-custom-setting-{{ id }}" class="codemirror" rows="10">{{ value }}</textarea>
</div>

<textarea{{ popover }} rows="{{ values_options.rows }}" name="custom_setting{{ name }}" id="input-custom-setting-{{ id }}" class="form-control" placeholder="{{ placeholder }}"{{ values_options.mask ? ' data-mask="'~values_options.mask ~'"' : '' }}>{{ value }}</textarea>
{% if error %}
    <div class="text-danger">{{ error }}</div>
{% endif %}

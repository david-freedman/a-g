<legend{{ popover }} data-sort="{{ sort_order }}" data-section="{{ section_code }}" data-setting="{{ setting_code }}" data-id="{{ id }}" id="input-custom-setting-{{ id }}"{{ not developer_status and not error and show_developer ? ' style="display:none"': '' }}>{{ title }}</legend>
